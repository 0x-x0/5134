language: node_js

node_js: 0.12

branches:
  only:
    - master
    - bar
#  except:
#    - bar

    
build:
  ci:
    - echo "Hello, World!"
    - echo "This is master"
    - echo "This is ??????"
    - echo "Hello, World!"
    - echo "$CI"
